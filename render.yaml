services:
  - type: web
    name: return-shield-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
      - key: DATABASE_URL
        fromDatabase:
          name: return-shield-db
          property: connectionString

databases:
  - name: return-shield-db
    databaseName: returnshield
    user: returnshield
    plan: free
